'use strict';
const expect = require('chai').expect;
const sinon = require('sinon');

describe('AtlasAccesslist', function() {
    beforeEach(function() {
        // Stub out a mock 
    });

    afterEach(function() {
        // restore original functionality
    });

    it('can do nothing', function() {

    });
});
